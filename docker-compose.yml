version: '3.9'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
  worker:
    build: .
    command: ["python", "-m", "apps.workers.run"]
    env_file:
      - .env
